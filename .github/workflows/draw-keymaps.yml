name: Draw ZMK keymaps
on:
  workflow_dispatch: # can be triggered manually
  push: # automatically run on changes to following paths
    paths:
      - "config/*.keymap"
      - "config/*.dtsi"
      - "keymap_drawer.config.yaml"
      # - 'boards/*/*/*.keymap'

jobs:
  draw:
    uses: caksoylar/keymap-drawer/.github/workflows/draw-zmk.yml@main
    permissions:
      contents: write # allow workflow to commit to the repo
    with:
      commit_message: "[Draw] ${{ github.event.head_commit.message }}"
      keymap_patterns: "config/*.keymap" # path to the keymaps to parse
      config_path: "keymap-drawer/config.yaml" # config file, ignored if not exists
      output_folder: "keymap-drawer" # path to save produced SVG and keymap YAML files
      parse_args: "" # map of extra args to pass to `keymap parse`, e.g. "corne:'-l Def Lwr Rse' cradio:''"
      draw_args: "anywhy_flake:'-d boards/shields/anywhy_flake/anywhy_flake.dtsi --select-physical-layout medium_layout'" # map of extra args to pass to `keymap draw`
